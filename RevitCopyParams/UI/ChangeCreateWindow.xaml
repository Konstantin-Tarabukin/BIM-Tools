<Window x:Class="RevitCopyParams.ChangeCreateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BIM Tools"
        Width="450"
        Height="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="150"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <!-- Описание -->
        <TextBlock Text="Описание изменения:"
                   FontSize="14"
                   Margin="0,0,0,8"/>


        <TextBox x:Name="DescriptionTextBox"
                 Grid.Row="1"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,0,0,12"/>


        <!-- Адресаты -->
        <StackPanel Grid.Row="2"
                    Margin="0,10,0,10">

            <TextBlock Text="Отправить разделам:"
                       FontSize="14"
                       Margin="0,0,0,5"/>

            <CheckBox x:Name="cbOV"
                      Content="ОВ"/>

            <CheckBox x:Name="cbVK"
                      Content="ВК"/>

            <CheckBox x:Name="cbEOM"
                      Content="ЭОМ"/>

            <CheckBox x:Name="cbAR"
                      Content="АР"/>

        </StackPanel>


        <!-- Кнопки -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">

            <Button Content="OK"
                    Width="90"
                    Margin="0,0,10,0"
                    Click="Ok_Click"/>

            <Button Content="Отмена"
                    Width="90"
                    Click="Cancel_Click"/>

        </StackPanel>


    </Grid>

</Window>